---
title: Copy
description: The Copy component is a button that copies text to the clipboard.
icon: Copy
---

<PoweredBy packages={[
  { name: 'React', url: 'https://reactjs.org/' },
]} />

<Preview name="buttons/copy" code={`'use client';

import { Copy } from '@/components/ui/dedevs-ui/buttons/copy';

const Example = () => {
  return (
    <div className="flex items-center gap-2 flex-col justify-center items-center h-screen w-full mx-auto bg-primary gap-4 text-primary-foreground">
      <Copy title="" textToCopy="npx dedevs-ui@latest add buttons/copy" textOnCopy="" />
      <code className="text-sm border rounded-md p-2 bg-primary/10 font-mono">npx dedevs-ui@latest add buttons/copy</code>
    </div>
  );
};  

export default Example;
`} />

## Installation

<Installer packageName="buttons/copy" />

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `textToCopy` | `string` | `'https://ui.dedevs.club/'` | The text to copy to the clipboard |
| `successDuration` | `number` | `1000` | The duration of the success animation |

<AutoTypeTable path="node_modules/@repo/buttons/copy.tsx" name="CopyProps" />
