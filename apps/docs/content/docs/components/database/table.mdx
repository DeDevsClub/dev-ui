---
title: Table
description: Table views are used to display data in a tabular format. They are useful for displaying large amounts of data in a structured way.
icon: TableProperties
---

<PoweredBy packages={[
  { name: 'TanStack Table', url: 'https://tanstack.com/tables/standard' },
  { name: 'Lucide', url: 'https://lucide.dev/' },
  { name: 'Jotai', url: 'https://jotai.org/' },
]} />

<Preview name="tables/standard" code={`'use client';

import {
  TableBody,
  TableCell,
  TableColumnHeader,
  TableHead,
  TableHeader,
  TableHeaderGroup,
  TableProvider,
  TableRow,
} from '@/components/ui/dedevs-ui/tables/standard';
import type { ColumnDef } from '@/components/ui/dedevs-ui/tables/standard';
import { ChevronRightIcon } from 'lucide-react';
import { exampleFeatures } from '@/lib/content';

const Example = () => {
  const columns: ColumnDef<(typeof exampleFeatures)[number]>[] = [
    {
      accessorKey: 'name',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Name" />
      ),
      cell: ({ row }) => (
        <div className="flex items-center gap-2">
          <div className="relative">
            <img
              src={row.original.owner.image}
              alt={row.original.owner.name}
              width={24}
              height={24}
              className="h-6 w-6 rounded-full"
            />
            <div
              className="absolute right-0 bottom-0 h-2 w-2 rounded-full ring-2 ring-background"
              style={{
                backgroundColor: row.original.status.color,
              }}
            />
          </div>
          <div>
            <span className="font-medium">{row.original.name}</span>
            <div className="flex items-center gap-1 text-muted-foreground text-xs">
              <span>{row.original.product.name}</span>
              <ChevronRightIcon size={12} />
              <span>{row.original.group.name}</span>
            </div>
          </div>
        </div>
      ),
    },
    {
      accessorKey: 'startAt',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Start At" />
      ),
      cell: ({ row }) =>
        new Intl.DateTimeFormat('en-US', {
          dateStyle: 'medium',
        }).format(row.original.startAt),
    },
    {
      accessorKey: 'endAt',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="End At" />
      ),
      cell: ({ row }) =>
        new Intl.DateTimeFormat('en-US', {
          dateStyle: 'medium',
        }).format(row.original.endAt),
    },
    {
      id: 'release',
      accessorFn: (row) => row.release.id,
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Release" />
      ),
      cell: ({ row }) => row.original.release.name,
    },
  ];

  return (
    <TableProvider columns={columns} data={exampleFeatures} className="bg-primary">
      <TableHeader>
        {({ headerGroup }) => (
          <TableHeaderGroup key={headerGroup.id} headerGroup={headerGroup}>
            {({ header }) => <TableHead key={header.id} header={header} className="text-white" />}
          </TableHeaderGroup>
        )}
      </TableHeader>
      <TableBody>
        {({ row }) => (
          <TableRow key={row.id} row={row}>
            {({ cell }) => <TableCell key={cell.id} cell={cell} className="text-white" />}
          </TableRow>
        )}
      </TableBody>
    </TableProvider>
  );
};

export default Example;`} />

## Installation

<Installer packageName="tables/standard" />

## Features

- Customizable columns
- Sortables/standard columns

## Examples

### Simple version

<Preview name="tables/standard" code={`'use client';

import {
  TableBody,
  TableCell,
  TableColumnHeader,
  TableHead,
  TableHeader,
  TableHeaderGroup,
  TableProvider,
  TableRow,
} from '@/components/ui/dedevs-ui/tables/standard';
import type { ColumnDef } from '@/components/ui/dedevs-ui/tables/standard';
import { ChevronRightIcon } from 'lucide-react';
import { exampleFeatures } from '@/lib/content';

const Example = () => {
  const columns: ColumnDef<(typeof exampleFeatures)[number]>[] = [
    {
      accessorKey: 'name',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Name" />
      ),
    },
    {
      accessorKey: 'status',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Status" />
      ),
      cell: ({ row }) => row.original.status.name,
    },
    {
      accessorKey: 'product',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Product" />
      ),
      cell: ({ row }) => row.original.product.name,
    },
    {
      accessorKey: 'startAt',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Start At" />
      ),
      cell: ({ row }) =>
        new Intl.DateTimeFormat('en-US', {
          dateStyle: 'medium',
        }).format(row.original.startAt),
    },
    {
      accessorKey: 'endAt',
      header: ({ column }) => (
        <TableColumnHeader column={column} title="End At" />
      ),
      cell: ({ row }) =>
        new Intl.DateTimeFormat('en-US', {
          dateStyle: 'medium',
        }).format(row.original.endAt),
    },
    {
      id: 'release',
      accessorFn: (row) => row.release.id,
      header: ({ column }) => (
        <TableColumnHeader column={column} title="Release" />
      ),
      cell: ({ row }) => row.original.release.name,
    },
  ];

  return (
    <TableProvider columns={columns} data={exampleFeatures} className="bg-primary">
      <TableHeader>
        {({ headerGroup }) => (
          <TableHeaderGroup key={headerGroup.id} headerGroup={headerGroup}>
            {({ header }) => <TableHead key={header.id} header={header} className="text-white" />}
          </TableHeaderGroup>
        )}
      </TableHeader>
      <TableBody>
        {({ row }) => (
          <TableRow key={row.id} row={row}>
            {({ cell }) => <TableCell key={cell.id} cell={cell} className="text-white" />}
          </TableRow>
        )}
      </TableBody>
    </TableProvider>
  );
};

export default Example;`} />

## Props

The Table component is made up of the following subcomponents:

### TableProvider

The `TableProvider` component is the root component of the Table. It contains the context for the other components.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableProviderProps" />

### TableHeader

The `TableHeader` component is a container for the column headers of the Table.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableHeaderProps" />

### TableHeaderGroup

The `TableHeaderGroup` component is a container for the column headers of the Table.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableHeaderGroupProps" />

### TableHead

The `TableHead` component is a single column header in the Table.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableHeadProps" />

### TableBody

The `TableBody` component is a container for the rows of the Table.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableBodyProps" />

### TableRow

The `TableRow` component is a single row in the Table.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableRowProps" />

### TableCell

The `TableCell` component is a single cell in the Table.

<AutoTypeTable path="node_modules/@repo/tables/standard.tsx" name="TableCellProps" />
