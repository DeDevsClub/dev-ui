---
title: Dropdown
description: The Dropdown component is a simple dropdown that allows users to select an option. It provides a clear, hierarchical view of tasks, allowing you to easily identify manage project timelines.
icon: Lock
---

<PoweredBy packages={[
  { name: 'React', url: 'https://reactjs.org/' },
]} />

<Preview name="select/dropdown" code={`'use client';

import { useState } from 'react';
import { DropdownSelect } from '@/components/ui/dedevs-ui/select/dropdown';

const Example = () => {
  const [password, setPassword] = useState('');

  return (
    <div className="flex w-full h-screen items-center justify-center bg-primary text-black p-8">
      <PasswordInput
        label="Enter your password"
        value={password}
        setValue={setPassword}
      />
    </div>
  );
}

export default Example;
`} />

## Installation

<Installer packageName="select/dropdown" />


## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `defaultValue` | `string` | `''` | The default value of the dropdown |
| `items` | `{ label: string; value: string }[]` | `[]` | The items of the dropdown |
| `onValueChange` | `(value: string) => void` | `undefined` | The function to set the value of the dropdown | 
| `placeholder` | `string` | `'Select'` | The placeholder of the dropdown |
| `disabled` | `boolean` | `false` | Whether the dropdown is disabled |
| `className` | `string` | `''` | The className of the dropdown |
| `isControlled` | `boolean` | `false` | Whether the dropdown is controlled |

<AutoTypeTable path="node_modules/@repo/select/dropdown.tsx" name="DropdownSelectProps" />
