---
title:  Input Password
description: The Input Password component is a simple input field that allows users to enter a password. It provides a clear, hierarchical view of tasks, allowing you to easily identify manage project timelines.
icon: Lock
---

<PoweredBy packages={[
  { name: 'React', url: 'https://reactjs.org/' },
]} />

<Preview name="database/input/password" code={`'use client';

import { useState } from 'react';
import { PasswordInput } from '@/components/ui/dedevs-ui/database/input/password';

const Example = () => {
  const [password, setPassword] = useState('');

  return (
    <div className="flex w-full h-screen items-center justify-center bg-primary text-black p-8">
      <PasswordInput
        label="Enter your password"
        value={password}
        setValue={setPassword}
      />
    </div>
  );
}

export default Example;
`} />

## Installation

<Installer packageName="database/input/password" />


## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | `'Password'` | The label of the input field |
| `value` | `string` | `''` | The value of the input field |
| `setValue` | `(value: string) => void` | `undefined` | The function to set the value of the input field | 

<AutoTypeTable path="node_modules/@repo/database/input/password.tsx" name="PasswordInputProps" />
